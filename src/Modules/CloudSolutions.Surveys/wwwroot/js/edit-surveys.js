/*
** NOTE: This file is generated by Gulp and should not be edited directly!
** Any changes made directly to this file will be overwritten next time its asset group is processed by Gulp.
*/

document.addEventListener('DOMContentLoaded', function () {
  var enableDailyLimit = document.getElementById('SurveyRestrictionPart_EnableDailyLimit_Value');
  var dailyMaxWrapper = document.getElementById('SurveyRestrictionPart_MaxDailyAllowed_Value_FieldWrapper');
  if (enableDailyLimit && dailyMaxWrapper) {
    enableDailyLimit.addEventListener('change', function (e) {
      if (e.target.checked) {
        dailyMaxWrapper.classList.remove('d-none');
      } else {
        dailyMaxWrapper.classList.add('d-none');
      }
    });
    enableDailyLimit.dispatchEvent(new Event('change'));
  }
  var enableWeeklyLimit = document.getElementById('SurveyRestrictionPart_EnableWeeklyLimit_Value');
  var weeklyMaxWrapper = document.getElementById('SurveyRestrictionPart_MaxWeeklyAllowed_Value_FieldWrapper');
  if (enableWeeklyLimit && weeklyMaxWrapper) {
    enableWeeklyLimit.addEventListener('change', function (e) {
      if (e.target.checked) {
        weeklyMaxWrapper.classList.remove('d-none');
      } else {
        weeklyMaxWrapper.classList.add('d-none');
      }
    });
    enableWeeklyLimit.dispatchEvent(new Event('change'));
  }
});